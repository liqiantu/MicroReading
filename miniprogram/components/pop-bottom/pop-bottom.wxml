<!--components/pop-bottom/pop-bottom.wxml-->
<view wx:if="{{isShow}}" class="bg" bindtap="onBgTap">
	<view class="wrap {{isShow ? 'show' : 'hide'}}">
		<view class="content" hidden="{{!isShow}}">
		</view>
	</view>
</view>