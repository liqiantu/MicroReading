<view class="wrap">
	<block wx:for="{{list}}">
		<view class="item" bindtap="itemTap" data-idx="{{index}}">
			<image src="{{item.CoverImages[1]}}"></image>
			<text>{{item.Name}}</text>
		</view>
	</block>
</view>